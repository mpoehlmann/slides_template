%-------------------------
% Defines the appearance of any items that make up the slides central content e.g., lists or the title on the title page
%-------------------------

\mode<presentation>


% Arguments
\DeclareOptionBeamer{group}{\def\beamer@michael@group{#1}}
\ProcessOptionsBeamer


% Title page
\defbeamertemplate*{title page}{michael}[1][]
{
  \begin{tikzpicture}[remember picture,overlay]
    \node[at=(current page.center), fill=primary, minimum width=\paperwidth, minimum height=\paperheight] {};
  \end{tikzpicture}
  \vfill
  \begingroup
    \centering
    \begin{beamercolorbox}[left, leftskip=2\edgespacing, #1]{title}
      \usebeamerfont{title}\inserttitle\par%
      \ifx\insertsubtitle\@empty%
      \else%
        \vskip0.25em%
        {\usebeamerfont{subtitle}\insertsubtitle\par}%
      \fi%
    \end{beamercolorbox}
    \begin{beamercolorbox}[wd=\paperwidth, colsep=1pt]{title separation line}\end{beamercolorbox}
    \vskip1em\par
    \begin{beamercolorbox}[left, leftskip=2\edgespacing, #1]{title}
      \usebeamerfont{author}\insertauthor
    \end{beamercolorbox}
    % \ifx\beamer@michael@group\@empty%
    % \else%
    %   \begin{beamercolorbox}[left, leftskip=2\edgespacing, #1]{title}
    %     % \usebeamerfont{institute}\insertinstitute
    %     \vskip0.125em%
    %     \usebeamerfont{group}\beamer@michael@group
    %   \end{beamercolorbox}
    % \fi%
  \endgroup
}


\mode<all>